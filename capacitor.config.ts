import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.starplus.com',
  appName: 'Star+',
  webDir: 'build/',
  bundledWebRuntime: false
};

export default config;
